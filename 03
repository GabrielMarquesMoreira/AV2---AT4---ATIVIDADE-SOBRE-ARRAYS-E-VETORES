#include <stdio.h>

int main() {
    int v[5], cont = 0;

    for(int i = 0; i < 5; i++) {
        scanf("%d", &v[i]);

        if(v[i] > 0) {
            cont++;
        }
    }

    printf("Positivos: %d", cont);

    return 0;
}
