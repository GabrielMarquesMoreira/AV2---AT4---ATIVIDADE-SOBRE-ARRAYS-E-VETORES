#include <stdio.h>

int main() {
    int v[5], cont = 0;

    for(int i = 0; i < 5; i++) {
        scanf("%d", &v[i]);

        if(v[i] > 10) {
            cont++;
        }
    }

    printf("Quantidade = %d", cont);

    return 0;
}
